language: java
dist: trusty
jdk:
    - oraclejdk8
    - openjdk8
install:
    - >
        mvn install
        --define "skipTests=true"
        --define "maven.javadoc.skip=true"
        --batch-mode
        --show-version
        --file duo-client/pom.xml
script:
    - >
        mvn test
        --batch-mode
        --file duo-client/pom.xml
    - >
        mvn checkstyle:check
